extends ../layout

block content
    h1= title
    div
      form
        fieldset
          label Название
          input(type='text', disabled=true, value=provider.prov_name)
          br
          label Адрес
          input(type='text', disabled=true, value=provider.addres)
